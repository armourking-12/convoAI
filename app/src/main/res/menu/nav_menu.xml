<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@+id/group_chat">
        <item android:id="@+id/nav_new_chat" android:icon="@android:drawable/ic_menu_add" android:title="New Chat" />
        <item android:id="@+id/nav_history" android:icon="@android:drawable/ic_menu_recent_history" android:title="Chat History" />
    </group>
    <item android:title="Preferences">
        <menu>
            <item android:id="@+id/nav_dark_mode" android:icon="@android:drawable/ic_menu_day" android:title="Dark Mode" app:actionLayout="@layout/switch_item" />
        </menu>
    </item>
    <group android:id="@+id/group_footer">
        <item android:id="@+id/nav_settings" android:icon="@android:drawable/ic_menu_preferences" android:title="Settings" />
    </group>
</menu>